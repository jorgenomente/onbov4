# ADD OPENAI MODEL CHEAP

## Contexto

Configurar un modelo OpenAI barato en `.env.local` para habilitar el provider real.

## Prompt ejecutado

```txt
si agrega uno que sea barato
```

Resultado esperado

Agregar `OPENAI_MODEL` en `.env.local` con un modelo económico.
